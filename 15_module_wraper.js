

// (function(exports, require, module, __filename, __dirname){
    //Inside IIF(immediate invoked function) scope is private or local
    
// })()

const name = "keyur";
    console.log("name is ", name);